# VF-M2 adaptive evidence gates — local PDCA

Status: `SHADOW / LOCAL_PR_READY_CANDIDATE`; no push or activation.

## Plan

Implement the approved PANSPHAIRA-native design after the serial delivery is
terminal 3/3: closed declarative schema, adaptive selector, registered verifier,
fresh receipts, parent reverification, delivery state machine, read-only
conveyor boundary, and three-dimensional status truth.

Acceptance evidence is a focused contract suite plus the unchanged repository
suite. The authoritative comparator remains `npm test`.

## Do

The implementation adds one TypeScript contract module, two schemas, two
closed CLIs, focused tests and operator/development documentation. It modifies
only the contracts index and package scripts. No production evidence is used
as completion evidence, and no public or runtime state is written.

## Check

The focused suite proves deterministic profile selection, closed schemas,
fresh/tamper-evident receipts, repeated exact checks, delegated parent rerun,
scope binding, ordered delivery, separate product evidence, deadman semantics,
external-wait honesty, and safe evaluator boundaries. Full-suite and integrity
results are recorded in the detached canary evidence directory rather than in
this product document.

## Act

Keep the feature in Shadow and submit only for independent local re-verification.
Do not reduce the full suite, install hooks, write conveyor state, or infer
external authority. Fallback is a one-commit revert while retaining VF v1/v2
and all historical evidence.
