# ChimpMaera v0.2 source-hygiene risk decision — PDCA

Status: COMPLETE LOCALLY

Branch: `feat/v0.2-source-hygiene-risk-decision`

Baseline: `b548b3d8b25448d078457e30d2b0d17edfa70162`

## Autonomy gate

The adversarial review metric is already complete at **4/4** and was not
optimized again. This phase started with the concrete metric
**source-hygiene risk decisions 0/2** and closes it at **2/2**:

1. classify every one of the 32 complete-diff findings by byte and evidence
   scope;
2. record and apply a conservative fix-or-defer decision with risk, fallback
   and review markers.

Reachability was local: the checkpointed branch existed cleanly at `b548b3d`,
the prior worker was finished, `git diff --check` reproduced all findings, and
the public manifest plus `SHA256SUMS` exposed the byte boundary without any
network, owner or publication action.

- Implementable: classification, repository-only cleanup, exact negative
  recheck and durable local evidence.
- Preparable only: an owner-facing handoff for a future authorized remote
  review candidate.
- Externally gated: push, pull request, merge, tag, release, publication, live
  Paperless/provider compatibility and external supply-chain claims.

## Plan / Do

The 32 findings separate into two different risk classes:

| Scope | Findings | Classification | Action |
|---|---:|---|---|
| nine `docs/development/` files | 24 | 20 Markdown hard-break spaces plus four extra EOF blank lines; excluded from the public manifest and `SHA256SUMS` | correct now using ordinary paragraphs and one terminal newline |
| eight public-manifest files | 8 | extra EOF blank lines in three runtime sources, two contract sources, two tests and one public document; all checksum-covered | defer to the next real public-byte change |

Decision `AD-HYG-001`: correct repository-only evidence now. Purpose: remove
24 findings without invalidating public or runtime evidence. Risk: metadata
layout can render differently. Fallback: revert this repository-only commit.
Review marker: first owner-authorized review of the development packet.

Decision `AD-HYG-002`: defer the eight public EOF normalizations until the next
real correcting public byte change. Purpose: avoid checksum regeneration and a
second full install/demo smoke solely for semantics-neutral formatting. Risk:
the complete baseline diff remains non-clean with exactly eight known
findings. Fallback: if a required gate makes them blocking, normalize all eight,
refresh `SHA256SUMS`, run focused checks and perform the then-justified
post-change full smoke. Review marker: next runtime, contract, public-doc or
public-test byte change.

Machine-readable classification is in
`docs/development/evidence/v0.2-source-hygiene-risk-decision-20260801.json`.

## Check

The phase must satisfy all of these local assertions before commit:

- the correction diff itself passes `git diff --check`;
- the complete `e698946..candidate` diff reports exactly eight findings and
  every path is one of the recorded public files;
- no public-manifest path, runtime byte, installer byte or `SHA256SUMS` entry
  changes in this phase;
- the source-hygiene evidence JSON parses successfully.

The integrated install/demo smoke is intentionally not repeated because this
phase changes only repository-excluded development evidence. The existing
`SAFE_DEMO_COLD-03` result remains the frozen-byte evidence.

## Act

The 2/2 decision metric is closed and must not be optimized again. The next
quality phase is local checkpoint durability: make the repository locator,
branch tip and recovery proof independently reproducible without consulting a
session transcript. External actions remain separately gated.
