# ChimpMaera v0.2 Wave 4 — durable checkpoint

Status: IN PROGRESS — implementation/fast gates pass; final smoke pending  
Branch: `feat/v0.2-approval-workbench`

| Milestone | Evidence gate | State |
|---|---|---|
| W4-M0 plan | declared claims, negatives, rollback and final-smoke rule | PASS |
| W4-M1 lock/verifier | offline artifact lock verification | PASS |
| W4-M2 closure | runtime/release coverage checks | PASS |
| W4-M3 fast evidence | mutation negatives, verifier and unit suite | PASS — 68/68 + 15/15 video |
| W4-M4 final freeze | integrated digest recorded | PASS |
| W4-M5 final smoke | one isolated W1–W4 install/demo smoke | PENDING |

Integrated full install count after scope extension: **0**. Historical Wave 1
count remains documented in its checkpoint and is not repeated here.

Evidence log before final smoke:

- Artifact lock: `demo/manifests/supply-chain/artifact-lock-v1.json`;
  verifier: `scripts/verify-supply-chain.mjs`; claim limits:
  `docs/SUPPLY-CHAIN.md`.
- Verifier PASS lock digest:
  `5fb2a2817a39d57c473e306a093758c66acde2d3f705fead4f75fbd775a51656`.
- Mutation fixtures deny a mutable OCI declaration, missing npm integrity,
  short CI action ref, omitted runtime module and omitted critical public file.
- CI npm was aligned from 11.11.0 to the already-declared `npm@11.16.0`.
- W1–W3 public files were added to the release manifest; development PDCA and
  generated evidence remain explicitly repository-only. Runtime image COPY
  closure covers every `demo/runtime/*.mjs` file.
- Full tests passed 68/68; video tests passed 15/15; lint, shell/Node syntax,
  `git diff --check` and all 120 repository checksums passed.
- Deterministic public release-tree build passed. Candidate archive evidence
  was created only in `/tmp`, not published or retained as a release claim.
- Frozen checksum-manifest digest across 120 non-development source files:
  `c551574d904d510ff43d58d0923ce84dfa83a8712b2c02aa4a64136b5ced2ecd`.

Autonomous decisions:

- AD-W4-001: align CI to the repository package-manager declaration. Risk:
  CI image npm bootstrap changes. Fallback: revert the one workflow line.
  Review marker: first CI run on a future authorized PR.
- AD-W4-002: keep `.github/`, generated local state and development PDCA out of
  the public release closure while still rejecting symlinks/special files.
  Risk: repository-only files are not inspected by public-content rules.
  Fallback: add a separate repository policy scanner; rollback is the builder
  condition change.
- AD-W4-003: represent Paperless as adapter-only with no OCI lock entry. Risk:
  callers may supply an ungoverned service. Fallback: stock enablement stays
  denied until a complete pinned service stack is added and evidenced.
