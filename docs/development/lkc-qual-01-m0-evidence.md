# LKC-QUAL-01 M0 local evidence

- Issue: `#116`
- Pinned base: `5a995b95670947967896dc703cd96796c1d2e89b`
- Risk lane: L2, security-sensitive
- Data: public, fictional purchasing fixture only
- Network/provider: none
- Authority: existing read-only Knowledge Envelope boundary; activation remains unauthorized

The focused evidence binds state-dependent JSON Schema/runtime parity, exact contribution-bound receipt validation, executable typed purchasing golden cases, strict configuration/context/model boundaries, deterministic qualification, applicability-first retrieval, fail-closed quarantine, exact LKG rollback or explicit denial, the default-off internal-network/loopback-only compose profile, and read-only API export. The owner-reserved repository-wide `npm test` suite was not run.

Expected focused commands after final bytes are bound:

```text
npm run knowledge-quality:test
npm run knowledge-quality:schema-parity
npm run knowledge-envelope:test
npm run lint
docker compose -f demo/knowledge-quality/compose.yaml config
npm run release-governance:verify
sha256sum --check SHA256SUMS
```

Evidence limitations: local tests are not CI, review, merge, release, deployment, production activation, or a global domain-quality claim. The API is a bounded local demonstration and the optional-model negative path is synthetic.
