# ChimpMaera v0.2 review-readiness — PDCA

Status: COMPLETE LOCALLY

Branch: `feat/v0.2-review-readiness`

Baseline: `f12dbea072178b99b50990bd16766563ac47b80d`

## Autonomy gate

The ordered implementation phases are complete at **4/4** and path-integrity
hardening is complete at **5/5**. Neither metric is optimized again. The
current phase is **v0.2 review-readiness**, with concrete metric **4/4** after
the exact reproduction commands passed against the frozen packet.

Reachability is proven locally: the prior worker is done, the isolated feature
branch is clean, and repository history, evidence, Node/npm tests, checksum
verification and public-tree builder are present. No remote or owner action is
needed for the four local milestones.

- Implementable: evidence index, exact reproduction, rollback audit and clean
  local handoff.
- Preparable only: an owner-facing description for a future authorized remote
  review candidate.
- Externally gated: push, PR, merge, tag, release, publication, live provider
  or Paperless claims and external supply-chain evidence.

| Milestone | Evidence gate | State |
|---|---|---|
| R1 evidence index | every phase maps to its implementation, PDCA/checkpoint and focused tests | PASS |
| R2 reproduction contract | exact local commands and expected outcomes are documented | PASS |
| R3 rollback audit | five dependency suffixes test clean; full rollback equals baseline tree | PASS |
| R4 clean handoff | packet commands pass, evidence is reconciled and local commits are clean | PASS |

## Plan / Do

The review packet is intentionally repository-only development evidence. It
does not alter runtime, install, public release or provider bytes. The evidence
index and handoff consolidate existing artifacts without promoting historical
smoke digests into release claims.

Rollback was tested in disposable local clones at `f12dbea`. Five profiles
were applied with `git revert --no-commit`; the resulting tests passed at
68/68, 66/66, 62/62, 59/59 and 50/50. The complete v0.2 rollback produced tree
`3b6cb6eca38cce792050d04fae028109b049aff2`, exactly equal to the
`e698946` baseline tree.

## Autonomous decisions

Decision `AD-REV-001`: keep review indexes under `docs/development/` and out
of the public manifest. Purpose: separate local reviewer evidence from public
product bytes and claims. Risk: an index can drift from implementation.
Fallback: treat executable checks and Git history as authoritative and refresh
the index on a real byte change. Review marker: first owner-authorized PR
review.

Decision `AD-REV-002`: document only dependency-suffix rollback boundaries.
Purpose: avoid leaving later integrations referring to removed earlier
contracts. Risk: a suffix rollback removes more functionality than a bespoke
patch. Fallback: use the next larger validated suffix, ultimately the exact
`v0.1.0` tree. Review marker: owner chooses the desired capability boundary
before any revert.

Decision `AD-REV-003`: do not repeat the integrated install/demo smoke.
Purpose: preserve the one-smoke-after-freeze rule. Risk: review-only documents
receive no install path exercise. Fallback: the already-passed frozen smoke is
the evidence; a second full run becomes justified only after a correcting
runtime or install byte change. Review marker: compare future diffs with
`1b9e6bc` before authorizing a smoke.

## Check

The review packet was frozen at `5fa6aed3cc64c5f61e7e57094d1fd82e328c6710`.
Its exact local reproduction passed: TypeScript lint, 70/70 project tests,
15/15 video tests, six supply-chain checks, 120/120 checksum entries and the
deterministic public-tree build. Temporary public-tree output was removed.

The fail-fast command batch reached every repository check successfully. A
report-only one-line formatter then raised a quoting `SyntaxError` after the
checks had completed. It could not affect repository results or bytes, so no
gate was repeated merely to regenerate presentation output.

The integrated install/demo smoke was not repeated: this phase changes only
repository-excluded development evidence and cannot exercise the runtime or
installer.

## Act

The 4/4 review-readiness metric is closed and must not be optimized again.
The packet is a clean local handoff. The next valuable phase is an adversarial
release-candidate review of claim/evidence consistency and rollback safety,
still local-only; any remote handoff or publication remains owner-gated.
