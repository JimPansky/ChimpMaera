# BI-006 Issue #15 E2E and evidence gate PDCA

Plan: compose accepted BI-001 through BI-005 behavior without widening their default-off, read-only, synthetic boundaries. Bind the exact protected base, immutable fixtures, model, values, negative outcomes, and honest non-claims.

Do: add one repository-native Node gate, deterministic focused test, sanitized evidence generator/index, operator guide, public-manifest entries, and checksum bindings. Atomic state and marker-verified reset make interruption and scope observable without touching source fixtures.

Check: `npm run bi-e2e:test` runs 9 tests and 44 assertions over healthy/ready setup, exact CRM/ERP/reconciliation/dashboard agreement, deterministic replay, scoped reset, interruption recovery, 15 named probes, fixture immutability, identity drift, and evidence tamper denial.

Act: keep the slice disabled by default. Roll back by disabling connectors and services, deleting only marker-verified derived state, restoring base `28de5f9a3b914865b6e03ff197f6efc24906588c`, and retaining failures as negative evidence. Any claim, identity, fixture, model, tolerance, probe, state scope, or evidence convention change requires review.

Residual boundary: local deterministic fixtures cannot prove production or live-system compatibility, DMS behavior, audit/compliance assurance, real-time, load, SLA, release, deployment, or infrastructure behavior.
